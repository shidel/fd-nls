# Language: Danish
# File ending: dk
# Codepage: 865
# This translation was made by Google Gemini.
# Please help the FreeDOS group to improve it.

vfdutil [options]

Fil- og biblioteksv‘rkt›j.

    [none]      G›r intet.
    /D file     Beregn og udskriv drevbogstav for file til STDOUT.
    /P file     Beregn og udskriv sti for file til STDOUT.
    /C          Brug f›r /D eller /P til at ‘ndre drev eller sti uden
                tekstoutput.
    /X          Brug f›r /D eller /P til at returnere errorlevel 1-26
                for drevbogstav.
    /F file     Beregn og udskriv komplet filnavn til STDOUT.
    /B file     Beregn og udskriv filnavn med filtypenavn til STDOUT.
    /N file     Beregn og udskriv filnavn uden filtypenavn til STDOUT.
    /E file     Beregn og udskriv filens filtypenavn til STDOUT.
    /U template Beregn og udskriv unikt fil-/biblioteksnavn baseret p†
                template til STDOUT. Drevet og stien skal eksistere.
                Eksempel: "vfdutil /u C:\TEMP\TEST????.$$$"
    /S filespec S›g i PATH efter en given filespec og udskriv alle
                matchende filer til STDOUT. Det aktuelle bibliotek
                gennems›ges ikke. Hvis filespec ikke findes i stien,
                afsluttes med errorlevel 1.
    /T filespec Ligesom /S, undtagen at kun errorlevel returneres.
    /M location Udskriv m‘ngden af ledig diskplads for location.
    /R required Brug sammen med /M til at teste om m‘ngden af required
                plads er tilg‘ngelig. Required er begr‘nset til 65535.
                Men det kan tilf›jes med B, K, M, osv. Det returnerer
                et errorlevel p† 1, n†r der ikke er nok plads.
    /A path     Test om der findes filer eller biblioteker i en path,
                0=dir eksisterer og er ikke tom, andre v‘rdier er
                DOS-fejlkoder fra testen.

    tba         (Stadig under udvikling, mere vil blive annonceret)

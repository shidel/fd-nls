# Language: Portuguese
# File ending: pt
# Codepage: 860
# This translation was made by Google Gemini.
# Please help the FreeDOS group to improve it.

vfdutil [options]

Utilit rio de ficheiros e diret¢rios.

    [none]      N„o faz nada.
    /D file     Calcula e envia a letra da unidade de file para STDOUT.
    /P file     Calcula e envia o caminho de file para STDOUT.
    /C          Usa antes de /D ou /P para alterar a unidade ou o
                caminho sem sa¡da de texto.
    /X          Usa antes de /D ou /P para retornar o errorlevel 1-26
                da letra da unidade.
    /F file     Calcula e envia o nome completo do ficheiro para STDOUT.
    /B file     Calcula e envia o nome do ficheiro com extens„o para
                STDOUT.
    /N file     Calcula e envia o nome do ficheiro sem extens„o para
                STDOUT.
    /E file     Calcula e envia a extens„o do ficheiro para STDOUT.
    /U template Calcula e envia um nome £nico de ficheiro/dir baseado no
                template para STDOUT. A unidade e o caminho devem
                existir. Exemplo: "vfdutil /u C:\TEMP\TEST????.$$$"
    /S filespec Pesquisa no PATH pela filespec dada e envia todos os
                ficheiros correspondentes para STDOUT. O diret¢rio atual
                n„o ‚ pesquisado. Se a filespec n„o for encontrada no
                caminho, sai com errorlevel 1.
    /T filespec Exatamente como /S, exceto que apenas retorna errorlevel.
    /M location Envia a quantidade de espa‡o livre em disco para
                location.
    /R required Usa com /M para testar se a quantidade required de
                espa‡o est  dispon¡vel. Required est  limitado a 65535.
                Mas pode ser sufixado com B, K, M, etc. Retornar  um
                errorlevel 1 quando n„o houver espa‡o suficiente.
    /A path     Testa se existem ficheiros ou diret¢rios num path,
                0=dir existe e n„o est  vazio, outros valores s„o
                c¢digos de erro DOS do teste.

    tba         (Ainda em desenvolvimento, mais ser  anunciado)

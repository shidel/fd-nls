# vask, de, cp858

vask [Optionen]

Aufforderung zur Texteingabe.

  [keine]     Verwendet erkannte Einstellungen fr die Eingabe.
  [text]      Voreingestellter bearbeiteter Text.
  /S          Bietet eine einfache Auswahl an Zeichenoptionen. Wie die
              Standardauswahl. Wenn verwendet, gibt vask nur die
              Fehlerstufe der Auswahl zurck.
  /A n        Setzt das Textattribut auf n.
  /B color    Setzt das Hintergrundtextattribut auf Farbe (oder einen Wert).
  /F color    Setzt das Vordergrundtextattribut auf Farbe (oder einen Wert).
  /G          Text global eingeben und alle Rahmungen ignorieren.
  /L          Text lokal in Bezug auf die Rahmung eingeben (STANDARD).
  /K n        Verwendet n anstelle des Rahmenzeichens, um Grenzen
              anzuzeigen.
  /C          Wenn angegeben und STRG-C gedrckt wird, wird mit Errorlevel
              200 beendet und und der Standardwert zurckgegeben.
  /W width    berschreibt die Standardbreite des Balkens. (Standard ist
              vom Cursor bis zum Zeilenende).
  /T file ID  Suche die ID in der Datei und verarbeite Sie wie Befehls-
              zeilenoptionen. Weitere Optionen hinter diesem Schalter wer-
              den dazu verwendet, %1-%9 Variablen im Textstring zu fllen.
  /D seconds  Verz”gerung in Sekunden bis zum Zeitablauf der Eingabeauf-
              forderung und gibt den anf„nglichen Standardwert zurck
              (liefert Errorlevel 1).

  tba         (Noch in der Entwicklung, mehr wird noch bekanntgegeben).
